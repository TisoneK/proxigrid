# Backlog (append-only)

Open items for future sessions. Append at the bottom; never delete or
reorder. When an item is done, check it off and note the session/commit —
don't remove the line.

<!-- TEMPLATE — copy below the last entry:
---
- [ ] **<short title>** (added YYYY-MM-DD by <agent>) — <enough context that
      a fresh agent can act on this without any chat history. Severity if known.>
-->

---
- [ ] **Remaining prisma-chain security advisory** (added 2026-08-26 by Claude Code) — 3 high-severity advisories persist after `npm audit fix --force`: `prisma` → `@prisma/config` → `deepmerge-ts <8.0.0` (GHSA-ggr8-5vv4-36mx, stack exhaustion on recursive-object-graph merges). Build-time CLI tooling; `prisma`/`@prisma/client` are at 6.19.3. The only fix npm offers is a prisma dev pre-release, so it was left in place. Recheck when Prisma ships a stable release whose `@prisma/config` pins `deepmerge-ts@>=8`.
- [x] **Pre-existing lint errors: setState-in-effect** (added 2026-08-26 by Claude Code) — `npm run lint` reported 5 errors (`react-hooks/set-state-in-effect`) in `count-up.tsx`, `header.tsx`, `carousel.tsx`, and `use-mobile.ts`. Fixed in commit cdd4576 via `useSyncExternalStore` (media query / clock / Embla) and derived render state (count-up); lint clean, build green. (done 2026-08-26, Session 3)

---
- [x] **Upgrade deprecated packages** (added 2026-08-27 by Kiro) — Two deprecated packages were flagged during npm install: `recharts@2.15.4` (1.x and 2.x branches no longer active, upgrade to v3 needed per https://github.com/recharts/recharts/wiki/3.0-migration-guide) and `eslint@9.39.5` (version no longer supported per https://eslint.org/version-support). Low severity but should be addressed to stay current with security patches and bug fixes. **Partially done (2026-08-26, Session 5, commit d6986e6): recharts → 3.10.1 (chart.tsx ported to v3 types; +react-is ^19). eslint deferred — see item below.**
- [x] **Markets grid has no error state on data-fetch failure** (added 2026-08-26 by Claude Code, Session 6) — When `/api/markets/[exchange]/ticker` fails (e.g. Binance HTTP 451 geo-block → 500), the Markets grid (`src/components/dashboard/`, the MarketGrid) shows loading skeletons indefinitely with no error/empty state surfaced to the user. Contrast the Portfolio panel, which shows a clean "Unconfigured: binance" message. Add an error/empty state to the market grid (surface the API error + a retry). Medium/UX severity. **Done (2026-08-26, Session 7, commit 05543e0): added an Offline badge + error/empty panel with a Retry button, shown whenever the query settles with no data (robust to dev StrictMode swallowing react-query's isError). Verified live against a broken endpoint.**
- [ ] **Binance User Data Streams + listenKey lifecycle** (added 2026-08-26 by Claude Code, Session 8) — Not implemented. For real-time execution reports and balance updates, add a private user WebSocket stream: `POST /api/v3/userDataStream` to get a listenKey, keep it alive with a `PUT` ping every ~30 min (expires after 60), and `DELETE` on shutdown (manual §4). Would let AutomationService react to fills without polling. Needs API credentials.
- [ ] **Binance FIX protocol connectivity** (added 2026-08-26 by Claude Code, Session 8) — Out of scope for now; institutional low-latency order entry over persistent TCP (manual §1). Only worth it for HFT use cases; the REST/WS adapter covers current needs.
- [ ] **Extend test coverage to services + adapter** (added 2026-08-26 by Claude Code, Session 9) — Vitest is set up (`npm test`); pure logic (indicators, filters, signer) is covered. Next: the service layer (`intelligence-service` signal generators, `automation-service` cooldown/evaluate, rule `conditions`/`actions`) and the Binance adapter's response mapping (incl. the kline→Candle regression) — these need light mocking of `db` (Prisma) and `BinanceClient`. Medium value.
- [ ] **eslint 10 blocked by Next lint stack** (added 2026-08-26 by Claude Code) — The whole eslint 9.x line (incl. latest 9.39.5) is flagged "no longer supported", so the deprecation can only be cleared by eslint 10. But `eslint-config-next@16`'s bundled `eslint-plugin-react` caps its eslint peer at `^9.7` (no ^10) and **crashes `npm run lint` under eslint 10** (verified 2026-08-26). Kept on `^9` so lint keeps working. Recheck when `eslint-config-next` / `eslint-plugin-react` ship eslint-10 support, then bump `eslint` to `^10` and re-run `npm run lint`.

<!-- ============ FEATURE ROADMAP (added 2026-08-28, Session 29/30) ============ -->

- [x] **Strategy backtester** ⭐ (added 2026-08-28 by Claude Code, from user's mockup) — **Done (2026-08-28, Session 32): lib/backtest.ts + BacktestPanel (MA crossover / RSI reversion, param sliders, chart w/ buy/sell markers, Total Return/Win Rate/Total Trades); vitest @/ alias config added; 27 tests green.** Original: A panel to test a strategy over historical candles before automating it. Pick a strategy (MA Crossover / RSI Reversion), tune params (Fast/Slow MA lookback, RSI thresholds) with sliders, run it over N candles, and show: the price chart with buy/sell markers, plus metrics — **Total Return, Win Rate, Total Trades** (matching the user's screenshot). Reuses `use-candles` + `lib/indicators`; add a `lib/backtest.ts` (pure) that simulates entries/exits and computes P&L, and a `BacktestPanel` component with the recharts chart + markers. High value — turns the app from "signals" into "validated strategies". No creds needed (historical only).
- [x] **Candlestick chart mode** (added 2026-08-28 by Claude Code) — **Done (2026-08-28, Session 33): PriceChart gained a `mode` prop (area|candles); candles via recharts ComposedChart + custom Bar shape (wick + open/close body, green/red), with a line/candles icon toggle in the coin detail dialog and an O/H/L/C tooltip.** Original: The coin detail chart is an area line; add an OHLC candlestick toggle (recharts custom shapes or a Candlestick series) so it reads like a real trading chart. Feeds the backtester's chart too.
- [x] **Price alerts** (added 2026-08-28 by Claude Code) — **Done (2026-08-28, Session 34): PriceAlertDialog composes a `price` condition + `notify` action via useCreateRule (no new backend); opened from a bell button in the coin detail header, seeds target with live price, above/below toggle, 1h cooldown. Verified end-to-end (rule appears in Automations).** Original: Simpler than indicator rules: "notify me when BTC crosses $X". The rules engine already supports a `price` condition (`lib/rules/conditions.ts`) — expose a lightweight "price alert" create flow (symbol + above/below + value) that reuses the automation engine + notify action. Consumer-friendly entry point.
- [x] **Configurable opportunity threshold** (added 2026-08-28 by Claude Code) — **Done (2026-08-28, Session 35): added `useLocalSetting` (localStorage + useSyncExternalStore) + `lib/settings.ts` + a header SettingsMenu popover with a sensitivity slider; OpportunityWatcher reads the threshold from it. Verified read/persist across reload.** Original: The OpportunityWatcher strength threshold is a hardcoded `0.5`. Make it a user setting (a `Setting` key-value table or localStorage) with a small control, so users tune alert sensitivity.
- [x] **Signal retention / pruning** (added 2026-08-28 by Claude Code) — **Done (2026-08-28, Session 36): generateAndPersist now dedupes — only writes when direction changed vs. the last stored signal for that (symbol,timeframe,indicator); added pruneOldSignals(SIGNAL_RETENTION_DAYS|7) run from the scanner tick. Verified: 2nd consecutive scan creates 0.** Original: The signal scanner persists every generated signal each pass (indicators × symbols × interval) → the `Signal` table grows unbounded. Add a retention job (delete signals older than N days, or cap rows) run from the worker, and/or dedupe identical consecutive signals.
- [x] **Rule execution detail view** (added 2026-08-28 by Claude Code) — **Done (2026-08-28, Session 37): ExecutionDetailDialog shows the trigger snapshot (symbol, price at fire, exchange/timeframe, matched conditions) + full action result (status/detail/raw JSON). Opened from notification-bell 'Rule fired' items and the automations table 'Last fired' line. Verified in-browser against a real fire.** Original: Executions are listed in the notifications/automations views but you can't inspect one. Add a detail (click an execution) showing the trigger snapshot (price, indicators at fire time) and the full action result. Data already persisted (`RuleExecution.triggerSnapshot`, `actionResult`).
- [ ] **Portfolio allocation chart** (added 2026-08-28 by Claude Code) — When an account is connected, show a donut/treemap of holdings by value (recharts) in the portfolio detail dialog. Needs real balances (creds + Binance-reachable host).
- [ ] **Multi-exchange support** (added 2026-08-28 by Claude Code) — The adapter registry is built to scale (`lib/exchanges/registry.ts` has commented Coinbase/Kraken/OANDA/Alpaca slots). Add a second adapter (e.g. Coinbase) to prove the abstraction and broaden market coverage.
- [ ] **Mobile / responsive polish** (added 2026-08-28 by Claude Code) — The dashboard is desktop-first. Audit the KPI grid, market rows (hidden columns), dialogs, and header at mobile widths; ensure the command palette, watchlist, and coin detail are usable on a phone.
- [ ] **Testnet order-path validation** (added 2026-08-28 by Claude Code) — The `placeOrder` path (filters/TIF/STP) is written but never run against a live matching engine. Once hosted where Binance is reachable, run a real testnet order via a `place_order` rule + the OrderConfirmDialog and fix whatever surfaces. Gates the whole "it actually trades" story.
