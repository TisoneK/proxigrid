# Review — 2026-09-04 (Session 66)

- **Agent:** ZCode | **Model:** glm-5.3-flash | **Platform:** Tisone's Windows workstation | **Role:** engineer
- **Target:** Complete the research loop per session 65's open items — history store, monitor pass, regime context.

## Executive summary

Three of the four session-65 open items are built, live-verified, and pushed: the `HistoricalCandle` store with an idempotent backfill route (1000 rows across 5 overlapping backfills = exactly 1000 rows), the research monitor wired into every scan tick (LIVE → MONITORING → DEGRADING transitions only on statistically meaningful deterioration; PAPER verdicts recorded, never transitioned), and regime distribution on every lab-run response. The fourth (richer Phase A grid) stays in the backlog — it's a strategy-design decision, not a wiring task. Also fixed a session-65 protocol slip: its open items were never appended to `tasks/backlog.md`; backfilled this session (append-only, 0 deletions).

## Commits

| Commit | Change |
|---|---|
| 4d4e140 | feat(research): HistoricalCandle store + backfill route |
| 06e92fc | feat(research): monitor pass on every scan tick + regime context in lab runs |
| + report/memory chore(context) commit | this session's .context/ updates |

## What was built

- **History store** (`src/lib/research/data/history-store.ts` + `POST|GET /api/research/history`): idempotent upserts on (exchange, symbol, timeframe, openTime); BigInt handling at the Prisma boundary; unique-key races from concurrent overlapping backfills swallowed (P2002 = row exists = goal achieved); reads shaped as `Candle[]` ready for `dataset.split()`. Hard cap 5000 bars/call.
- **Monitor service** (`src/lib/research/monitor/monitor-service.ts`): one pass over PAPER/LIVE/MONITORING strategies; fetches a recent 400-bar window per strategy; records `Experiment(kind:"monitor")` with the MonitorReport; transitions LIVE→MONITORING and MONITORING→DEGRADING through the guarded state machine; PAPER verdicts recorded only (the →LIVE promotion guard is the paper-trading judge). Specs that aren't named-strategy params are SKIPPED, not failed. Wired into `scanOnce()` — runs on both the self-hosted scanner and the Vercel cron tick.
- **Regime distribution**: `POST /api/research/run` responses now carry per-regime bar counts from the detector.

## Verification evidence

- `npx tsc --noEmit` exit 0 · `npm run lint` clean · `npx vitest run` **184/184** · `npm run build` exit 0 · pre-commit + exit gates passed.
- Live (dev server): backfill fetched+saved 1000 BTCUSDT 1h candles; read-back correct; 5 overlapping backfills → exactly 1000 stored rows (idempotency + race-tolerance verified); one transient 500 observed on a concurrent-ish overlap before the P2002 guard, three clean 200s after; lab run 200 with regime distribution `{LOW_VOL: 703, CONSOLIDATION: 122, RANGING: 61, UNCERTAIN: 61, TRENDING: 45, BREAKOUT: 8, HIGH_VOL: 0}` — the window context that explains the session-65 zero survivors; monitor pass smoke = clean no-op with 0 PAPER/LIVE strategies.
- HistoricalCandle rows intentionally left in the dev DB (they're real market data, useful for the next lab run).

## Open items (mirrored in tasks/backlog.md)

- Richer Phase A grid (Bollinger reversion, EMA trend + regime filter) — remaining item from session 65.
- Load candles for lab runs from the history store when available (live fetch fallback) — natural follow-up now that the store is populated.
- UI: surface regime distribution + monitor experiments in the Research Lab card.

## Notes

- Session-65 protocol slip logged and fixed: open items live in the backlog now, not only in the review report.
- No new flaws/inefficiencies: the transient backfill 500 was diagnosed and guarded in-commit rather than worked around.
