# Review — 2026-09-04 (Session 67)

- **Agent:** ZCode | **Model:** glm-5.3-flash | **Platform:** Tisone's Windows workstation | **Role:** engineer
- **Target:** Backlog items from session 66: history-first lab runs, regime/kind surfacing in the UI, richer Phase A grid.

## Executive summary

All three backlog items shipped. Lab runs now read from the HistoricalCandle store with live-fetch fallback + auto-backfill (verified: `candleSource: "history"` on the second live run); two new strategy families (Bollinger reversion, Donchian breakout) take the Phase A grid to 22 candidates across 4 named families + feature hypotheses; and the Research Lab card shows where data came from, the window's dominant regime, and each strategy's experiment-kind evidence trail. 188 tests green (4 new), build green, live-verified with dedup invariant intact (35 strategies = 35 distinct specHashes, 0 duplicates).

## Commits

| Commit | Change |
|---|---|
| 866cb84 | feat(research): bollinger_reversion + donchian_breakout strategy families |
| de40752 | feat(research): history-first lab runs + richer run context in the UI |
| + report/memory chore(context) commit | this session's .context/ updates |

## What was built

- **Two strategy families** in `engine/backtester.ts`: `bollinger_reversion` (buy lower-band pierce, exit through mean) and `donchian_breakout` (buy close above the prior N-bar high — window excludes the current bar so a monotonic rise doesn't self-break-out). Params on `StrategyParams`, perturbables in `backtestable.ts`, generators in `hypothesis/generator.ts`, 4 unit tests (signals fire on crafted series; flat/range stay silent).
- **History-first lab runs**: the run route loads from the store, falls back to live + backfills when short, `refreshHistory` override; response carries `candleSource`/`candles`; run toast shows source + dominant regime.
- **Evidence trail UI**: experiment-kind chips (oos/monitor/backtest, green/red by verdict) on each expanded strategy row.

## Verification evidence

- `npx tsc --noEmit` exit 0 · `npm run lint` clean · `npx vitest run` **188/188** · `npm run build` exit 0 · pre-commit gate passed.
- Live: run 1 — `source: history, candles: 1000, candidates: 22`, outcomes `{"critic":8,"scientist":14}` (0 survivors, honest gates); run 2 — 22 experiments persisted reusing all 22 specs; strategy/specHash invariant checked in DB: 35 rows, 35 distinct hashes, 0 dupes. Test strategies cleaned; stored candles kept (real data, useful next run).

## Open items (mirrored in tasks/backlog.md)

- Donchian breakout currently long-only by default config (`allowShort: false`); a `regime`-conditional short side is a design decision worth its own session.
- Multi-asset lab runs (assets[] sweep) — the pipeline is single-symbol per run today.
- All other backlog items unchanged (testnet order-path validation remains the gate to real trading).
